// Stub Database type — Supabase CLI access token unavailable in this environment.
// Replace with `npx supabase gen types typescript --project-id aludvzwmvxporkgbnotd > src/types/database.ts`
// once `supabase login` (or SUPABASE_ACCESS_TOKEN) is configured.
export type Database = {
  public: {
    Tables: Record<string, never>
    Views: Record<string, never>
    Functions: Record<string, never>
    Enums: Record<string, never>
  }
}
